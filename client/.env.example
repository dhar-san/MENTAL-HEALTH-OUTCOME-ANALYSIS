# Used at build time by Create React App (must start with REACT_APP_)
# Set this in Vercel: Project → Settings → Environment Variables → Production (and Preview if needed)

# Public URL of your Express API (no trailing slash). Local dev: leave unset to use package.json "proxy".
# Example: https://mental-health-api.onrender.com
REACT_APP_API_URL=
